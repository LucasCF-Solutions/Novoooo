const pontuacaoUsuario=990;
const nivelUsuario = pontuacaoUsuario >=1000 ? 'Usuario VIP' : 'Usuario normal'

const corUsuario = "white";
const corPadrao = corUsuario || 'preta'

console.log(corPadrao);
